# http{} context snippet for PortPax API (maps + rate limit).
# Install: sudo cp scripts/nginx/conf.d/portpax-api-maps.conf /etc/nginx/conf.d/
#
# Loaded automatically by nginx.conf include /etc/nginx/conf.d/*.conf;

# Soft rate limit for / (burst is applied in the site config).
limit_req_zone $binary_remote_addr zone=portpax_api:10m rate=40r/s;

# Scanner / abuse user-agents → $portpax_drop_ua = 1
map $http_user_agent $portpax_drop_ua {
    default                                                 0;
    ~*(libredtail|nikto|sqlmap|masscan|zgrab|nuclei|dirbuster|wfuzz|acunetix) 1;
}
